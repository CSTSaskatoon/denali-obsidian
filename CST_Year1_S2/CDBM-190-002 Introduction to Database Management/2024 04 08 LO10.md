### Useful Control Properties
*Column(column, row)* - will return value based on specified index, zero based, row is optional
*ListCount* - will return the number of items in a list
*Selected(index)* - sets or returns whether a row is selected
*ItemData(index)*
